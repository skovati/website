image: alpine/latest
packages:
  - hugo
  - hut
  - git
oauth: pages.sr.ht/PAGES:RW
environment:
  site: skovati.dev
tasks:
- build: |
    hugo -s website
    du -h website/public
- package: |
    tar -C website/public -czvf $site.tgz .
- publish: |
    hut pages publish -d $site $site.tgz
